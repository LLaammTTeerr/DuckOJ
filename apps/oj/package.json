{
  "name": "@duckoj/oj",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "oj": "./dist/main.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc -b && tsc --noEmit -p tsconfig.test.json",
    "lint": "eslint src test",
    "build": "tsc -b",
    "test": "tsc -b ../mcp && vitest run"
  },
  "dependencies": {
    "@duckoj/mcp": "workspace:^",
    "@duckoj/sdk": "workspace:^"
  }
}
