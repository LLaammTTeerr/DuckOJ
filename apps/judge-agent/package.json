{
  "name": "@duckoj/judge-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -b",
    "lint": "eslint src test",
    "test": "tsc -b && vitest run",
    "build": "tsc -b",
    "dev": "tsx watch src/main.ts"
  },
  "dependencies": {
    "@duckoj/api-prefix": "workspace:^",
    "@duckoj/observability": "workspace:^",
    "@duckoj/package-format": "workspace:^",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsx": "^4.23.12"
  }
}
