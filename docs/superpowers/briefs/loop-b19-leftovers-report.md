# B-19 — the leftovers (D102–D105)

Branch `worktree-agent-a717134c6b50ea612`, nothing pushed. **Seven commits, each red→green with its own mutation check** (`vitest run --no-file-parallelism`). Rulings **D102–D105**; **D47 and D85 amended in place**. Full ritual green.

**1. `must_change_password` is enforced by the API — `b89505a` + `088c2a0` (D102).** D61 left it to the web because reaching round it meant "driving the API by hand"; `oj login` and D89's MCP are that, documented. `TokenService.issue` refuses `409 password_change_required`; `resolve` refuses a token minted BEFORE the flag — the one population the mint check cannot reach. Reads with writes: a token is one credential, and splitting by verb would make every new write route inherit its protection from its method. Sessions untouched (they are how the change is made); listing and revoking still work. `LoginResponse` already carried the flag — pinned, since it is now load-bearing. `oj` answers the refusal ahead of each command's own guess: `watch` was five polls of "flaky judge" for an account whose remedy is a browser. MCP needed no change (B-17's `asHandlerError` carries code+detail through all three doors) and now has a test saying so. The WS upgrade reports an `AppError`'s status instead of 500. Mutations: both throws commented out; the WS branch removed.

**2. The crash-loop breaker needs two failed boots — `6267c14` (D103).** D85 was reasoned about with four workers, where a build that cannot boot is four exits. At `API_WORKERS=1` "a worker died" and "every worker is dead" are one event, so a single transient crash exited the primary and `deploy.sh` rolled back a healthy build. Now: empty fleet **and** ≥2 exits inside the window from workers that never reached `STABLE_UPTIME_MS` — the threshold already in the file. Multi-worker unchanged, pinned by a four-worker test. Mutation: the `>= MIN_EXITS` term dropped.

**3. `workers()` was already bounded — `2774b41`. RULING: the brief's premise was stale.** B-8 recorded it; **B-9's migration 0025 closed it**, and D47's amendment says so. It survived B-16/17/18 as "open" because only the *throughput* half was ever asserted. Both halves are in the plan spec now, same 100k fixture, same drop-the-index red direction: the live half reads `grading_jobs_active_idx` for 40 rows in **0.058 ms**; without the index the same statement seq-scans and discards **99 960** rows (**6.15 ms**). `'failed'` is a dead enum value, so `state <> 'done'` is bounded by work in flight. No migration — **0038 went to item 5**. One comment I wrote measured false and is corrected: an `in (...)` rewrite still gets the index; dropping the state term is what loses it.

**4. Four read swallows in `teams.tsx` — `70129fe`.** B-18 named three, F-25's team page added a fourth. All four go through `read()`. The team page keeps 404 as an answer (`absent: [404]`) — a 500 was telling a captain their team was disbanded. The edit form is not rendered at all over a failed load: `members` replaces the whole roster, so the empty box is the save that empties the team. Five tests; restoring all four `??` reddened four (the 404 test stayed green, correctly).

**5. The roster double-row has a constraint — `cc67903` (D104, migration 0038).** No index on an existing table can state it: for a team row `user_id` is only the captain and the people it seats live in `team_members`. `contest_seats (contest_id, user_id) PRIMARY KEY`, live rows only, written by every seating path — individual join (now transactional), `enterTeam` for **every** member, and the roster PATCH, which also **releases** the seats of members it removes (or D99's removed pupil is barred from the contest forever). The checks stay and keep naming the pupil; 23505 → the same `409 contest_already_joined`, never a 500. The race is pinned on two connections against a committed database — the two services each commit their own transaction, so the interleaving is issued as the two write sets. Mutation: drop the primary key and both commit. **The backfill is `ON CONFLICT DO NOTHING`, deliberately**: the defect has been reachable since D99, `runMigrations` runs at boot, and a violation there is an API that will not start rather than a data problem reported.

**6. The monitor's feed named the captain — `c57dad6` (D105).** Live probe as two real pupils: `bh19-b1` submitted and the feed said `bh19-a1`. D95 joined `users` through the participation, which stopped being the submitter when D99 made a team one row. Now `submissions.user_id`, with `team` beside it (null individually) because the board is keyed by team; the web prints the team as text, never a `/users/` link.

**Also fixed, and it is a finding: the api suite was RED at the merge base.** Two tests in `contest-teams.spec.ts` — F-25's `assertRosterUnlocked` landed after B-18's mid-round roster tests and refuses their editor with `team_locked_during_contest` before the check they exercise is reached. Confirmed by stashing everything and re-running. Both now edit as the organiser, which is F-25's own rule. Both loops reported their ritual green.

**Live probe** — 2 registrations, no token minted, nothing to revoke. `bh19-a1`/`bh19-b1`, org `bh19-school-1`, team `Đội BH19`, round `bh19-round-1`: join → teammate's 409 `contest_team_joined` → the NON-captain reads the problem and submits → **AC** → monitor counters (D100) 1/1/1/0, queue 0, judge online → scoreboard by team with the `teams` sidecar → 17 kB booklet → `results.csv` carrying `members` → clarification → mid-round removal refuses the submission `403 contest_not_joined` and the return restores it. `publishedVersion` is on live now, so B-16's concern is closed by the redeploy. One defect found: D105.

**Verify.** `-r typecheck`, `typecheck:scripts`, `-r lint`, `lint:scripts` green; contracts + SDK regen **no diff**; `vite build` green; `graphify update .` run. Every one of the twenty workspace packages run, api in its own `--no-file-parallelism` pass: api **121 files / 1071**, web 53/526, judged 128, contest-formats 120, mcp 90, prepare 62, package-format 54, db 49, similarity 43, glicko2 41, contracts 39, oj 35, polygon-import 19, judge-protocol 18, statement-samples 12, judge-agent 8, observability 4, sdk 2, api-prefix 1, realtime 1.

**Concerns.** (1) `contest_seats` is a guarantee about the future, not a proof about history: a deployment already holding a double seat keeps it, unseated on one side. (2) The roster PATCH now reads and writes a second table inside its transaction — bounded by contests entered, but it is a table that path did not touch before. (3) `team` widens `ContestMonitorEntry`, a contract addition made with nobody to ask. (4) D102 leaves `oj`/MCP unable to clear the flag at all, by design: the first refusal in this system whose remedy is not reachable from the CLI.
